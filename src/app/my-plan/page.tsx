import { WorkoutContext } from '@/context/WorkoutContext';
import React, { useContext } from 'react';

const page = () => {
    return (
        <div>
            
        </div>
    );
};

export default page;